# Netlify configuration — UMi Portail Psy (Next.js 14 App Router)
# The publish directory and serverless functions are handled by the plugin.

[build]
  command = "npm run build"

[[plugins]]
  package = "@netlify/plugin-nextjs"
